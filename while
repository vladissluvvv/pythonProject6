# # цикл while
#
# counter = 0
# while counter < 3:
#     print(f"счетчик: {counter}")
#     counter +=1
#
# password = ""
# while password != "secret":
#     password = input("введите пароль: ")
# print("доступ разрешен")
import random

# break - немедленный выход из цикла

# number = [1, 3, 5, 7, 9, 2, 4, 6, 8]
# search = 7
#
# for num in number:
#     print(f"Проверяю {num}")
#     if num == search:
#         print(f"найдено число {search}!")
#         break

# while True:
#     answer = input("введите выход для завершения: ")
#     if answer.lower() == "выход":
#         print("завершение программы")
#         break

# comtinue - пропуск текущей интерации
# for i in range(10):
#     if i % 2 != 0:
#         continue
#     print(f"четное: {i}")
#
# number = [5, -2, 10, -8, 3]
# for num in number:
#     if num < 0:
#         continue
#     print(f"положительное: {num}")

# numbers = [1, 2, 3, 4, 5]
# search = int(input("введите число из списка: "))
#
# for num in numbers:
#     if num == search:
#         print(f"число {search} найдено")
#         break
# else:
#     print(f"Число {search} не найдено в списке")

# вложенные циклы

# print("Таблица умножения: ")
# for i in range(1, 4):
#     for j in range(1, 4):
#         print(f"{i} x {j} = {i * j}")
#     print()
#
# list1 = [1, 2, 3]
# list2 = ['a', 'b', 'c']
# for num in list1:
#     for letter in list2:
#         print(f"({num}, {letter})")

# import random
#
# secret_number = random.randint(1, 100)
# attempts = 0
# max_attempts = 7
#
# print("я загадал число от 1 до 100. у тебя 7 попыток!")
#
# while attempts < max_attempts:
#     guess = int(input(f"Попытка {attempts + 1}. твоя догадка: "))
#     attempts += 1
#
#     if guess < secret_number:
#         print("Больше")
#     elif guess > secret_number:
#         print("меньше")
#     else:
#         print(f"поздравляю ты угадал за {attempts} попытка")
#         break
#     if attempts < max_attempts:
#         print(f"осталось попыток: {max_attempts - attempts}")
# else:
#     print(f"попытки закончились загаданное число было: {secret_number}")

# text = input("введите текст для анализа: ")
#
# vowels_count = 0
# consinants_count = 0
# digits_count = 0
# spaces_count = 0
#
# vowels = "аеёиоуыэюяaeiou"
#
# for char in text.lower():
#     if char.isalpha():
#         if char in vowels:
#             vowels_count += 1
#         else:
#             consinants_count += 1
#     elif char.isdigit():
#         digits_count += 1
#     elif char.isspace():
#         spaces_count += 1
# print("\n Результаты анализа:")
# print(f"Всего символов: {len(text)}")
# print(f"Гласных букв: {vowels_count}")
# print(f"Согласных букв: {consinants_count}")
# print(f"Цифр: {digits_count}")
# print(f"Пробелов: {spaces_count}")
# print(f"другие символы: {len(text) - vowels_count - consinants_count - digits_count - spaces_count}")

# генератор паролей

# import random
# import string
#
# print("генератор паролей")
# print("=" * 30)
# length = int(input("длина пароля (8-20): "))
# use_digits = input("использовать цифры? (да/нет): ").lower() == "да"
# use_symbols = input("использовать специальные символы? (да/нет): ").lower() == "да"
#
# characters = string.ascii_letters
#
# if use_digits:
#     characters += string.digits
# if use_symbols:
#     characters += "!@#$%^&*()_+-="
#
# password = ""
# for _ in range(length):
#     password += random.choice(characters)
#
# print(f"ваш пароль: {password}")
# print(f"длина: {len(password)} символов")
# print(f"набор символов: {len(characters)} вариантов")
#
# score = 0
# if any(char.islower() for char in password):
#     score += 1
# if any(char.isupper() for char in password):
#     score += 1
# if any(char.isdigit() for char in password):
#     score += 1
# if any(not char.isalnum() for char in password):
#     score += 1
# strength = ["слабый", "средний", "хороший", "отилчный"]
# print(f"сложность: {strength[score - 1] if score > 0 else 'очень слабый'} ")

# N = int(input("введите число N: "))
#
# print(f"Простые числа от 2 до {N}: ")
# for num in range(2, N + 1):
#     is_prime = True
#
#     for i in range(2, int(num ** 0.5) + 1):
#         if num % i == 0:
#             is_prime = False
#             break
#     if is_prime:
#         print(num, end="")
# print()

# n = int(input())
# for i in range(1, n + 1):
#     print("*" * i)

# n = int(input("введите число фактериала: "))
# if n < 0:
#     print("нельзя отлицательное число возвести в фактериал")
# else:
#     fa = 1
#     i = 1
#     while i <= n:
#         fa *= i
#         i += 1
# print(f"{n}! = {fa}")

# n = int(input("введите число фактериала: "))
# if n < 0:
#     print("нельзя отлицательное число возвести в фактериал")
# else:
#     fa = 1
#     for i in range(1,n + 1):
#         fa *= i
#     print(f"{n}! = {fa}")
